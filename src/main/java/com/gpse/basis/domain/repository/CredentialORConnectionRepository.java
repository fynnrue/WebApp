package com.gpse.basis.domain.repository;

import com.gpse.basis.domain.credentials.CredentialORConnection;
import org.springframework.data.repository.CrudRepository;

public interface CredentialORConnectionRepository extends CrudRepository<CredentialORConnection, Long> {
}
