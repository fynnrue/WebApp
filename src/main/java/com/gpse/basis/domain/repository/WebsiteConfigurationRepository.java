package com.gpse.basis.domain.repository;

import com.gpse.basis.domain.WebsiteConfiguration;
import org.springframework.data.repository.CrudRepository;
public interface WebsiteConfigurationRepository extends CrudRepository<WebsiteConfiguration, Long> {
}
